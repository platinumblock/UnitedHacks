class Goal {

    constructor() {

    }

}


/**
 * Recommends a new goal based on a list of activities that the user has performed recently.
 *
 * @param activities  a list of {@code Activity} objects (see activity.js).
 *
 * @return a {@code Goal} object for the user to perform.
 */
export function generateGoal(activities) {

}
